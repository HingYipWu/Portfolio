# Backend Environment Variables
# Copy this file and rename it to: .env

# MongoDB Connection String
# Get this from MongoDB Atlas:
# 1. Go to https://cloud.mongodb.com
# 2. Click "Connect" on your cluster
# 3. Choose "Connect your application"
# 4. Copy the connection string
# 5. Replace <password> with your actual database password
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/portfolio?retryWrites=true&w=majority

# JWT Secret for Authentication
# This should be a long random string
# You can use: keyboard mash, or generate one online
# IMPORTANT: Keep this secret and use the SAME value in development and production
JWT_SECRET=your_super_secret_random_string_here_change_this

# Server Port (for local development)
PORT=5000

# Environment
NODE_ENV=development


# INSTRUCTIONS:
# 1. Rename this file to: .env (remove .example)
# 2. Replace MONGODB_URI with your actual MongoDB connection string
# 3. Replace JWT_SECRET with a random string (make it long and complex)
# 4. Save the file
# 5. NEVER commit this file to Git (it's in .gitignore)
